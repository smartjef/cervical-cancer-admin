services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=http://172.238.122.159:8080/api
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://172.238.122.159:8080/api
    restart: always
